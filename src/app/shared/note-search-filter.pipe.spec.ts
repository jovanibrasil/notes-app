import { NoteSearchFilterPipePipe } from './note-search-filter.pipe';

describe('NoteSearchFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteSearchFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
